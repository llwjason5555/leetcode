#include<iostream>
#include<unordered_set>

using namespace std;

int main()
{
	/*unordered_set<int> u;
	u.insert(1);
	u.insert(3);
	u.insert(10);
	u.insert(4);
	for (auto it = u.begin(); it != u.end(); ++it)
		cout << *it << endl;*/
	char a[5] = "oo";
	cout << sizeof(a) << endl;
	return 0;
}